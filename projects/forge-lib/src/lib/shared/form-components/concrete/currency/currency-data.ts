import { FormData } from '../../abstract/form-data';

/**
 * Currency data attributes
 */
export class CurrencyData extends FormData {
    /**
     * Input mask
     */
    public inputMask: string;
}
